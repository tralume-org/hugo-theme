# AGENTS.md

> 这是一份给协作型 AI（下称“Agent”）的行为与输出规范，用于 **Tralume（Hugo 主题）** 项目。在不与具体任务冲突的前提下，请严格遵循本文件。

---

## 1. 语言与风格（必须）

* **默认语言：简体中文**。除非任务明确要求其他语言，否则一律使用简体中文回答。
* **文档语言一致性（必须）**：
  * `README.md`：仅使用英文撰写，禁止夹带中文标题/段落或中英对照（如 `xxx / 中文`、同一行用 `/` 拼接双语）。
  * `README.zh-Hans.md`：仅使用简体中文撰写，禁止夹带英文说明段落或中英对照（必要的产品名/代码标识/语言代码如 `Hugo`、`en-US`、`zh-Hans` 除外）。
  * `i18n/zh-Hans.yaml`：界面文案应以简体中文为主，避免遗留整句英文占位文案；如必须保留第三方产品/扩展名称，可在中文语境中引用。

---

## 2. 代码片段要求（必须）

* **所有代码片段必须配备中文注释**，说明用途、关键配置项与注意事项。
* 注释优先级：**意图 > 关键参数 > 易错点**。
* 对外文标识（API、方法名、配置键）**原样保留**，注释使用英文。
* 如涉及破坏性操作（删除、覆盖、迁移），必须在代码块**上方**以“⚠️ 注意”小节明确风险与回滚方式。
* 样例最小化：给出**能运行/能复现**的最小片段；避免冗长模板与无关依赖。

> 代码块示例规范（仅示例，不代表必须输出该内容）：
>
> ```bash
> # 说明：初始化 Hugo 站点（示例）。
> # 作用：创建基础目录结构，便于随后安装主题与测试构建。
> hugo new site mysite  # 创建站点
> ```

---

## 3. 信息架构与输出结构（建议）

* 推荐顺序：**结论先行 → 步骤/方案 → 解释/原理 → 附录/参考**。
* 每个回答尽量包含：

  1. **任务目标**（一句话）；
  2. **关键步骤**（3–7 条）；
  3. **常见坑**（可选，列出 2–3 条）；
  4. **后续扩展**（可选，指向下一步工作）。
* 列表项不超过两级；超过两级改为小节。

---

## 4. i18n（国际化）要求（必须）

本项目为 Hugo 主题，需天然支持多语言与本地化。

### 4.1 术语与措辞

* 建立**术语表（Glossary）**：收录常见 UI/技术词汇的中英映射（如“Primary/主色”“Elevation/海拔（阴影层级）”）。
* 风格一致：选用**通俗、上下文一致**的术语。

### 4.2 资源与目录（示例约定）

* `i18n/zh-Hans.yaml`、`i18n/en-US.yaml`：放置界面文案键值对。
* `exampleSite/config/_default/languages.toml`：多语言站点配置（语言权重、默认语言、菜单翻译等）。
* 模板中**坚决禁止**写死文案；统一使用 i18n key 调用。

### 4.3 文案编写规范

* **避免拼接句**（如把“前缀 + 变量 + 后缀”拆成若干 key），尽量一整句为一个 key，降低语序问题。
* **占位符**使用具名形式（如 `{{ .Count }}`），并在注释标注单位/语境（单复数、量词）。
* **日期/数字/货币**统一使用 Hugo/Go 的本地化格式化方法；不要将格式写死在字符串中。
* **RTL（从右到左）**：样式与图标方向尽量避免依赖字面方向，必要时通过逻辑属性（如 `margin-inline-start`）。

### 4.4 翻译流程与质量门槛

* 新增/修改文案时：

  1. 新增 i18n key；
  2. 更新中文与英文的翻译；
  3. 在 PR 描述中列出“新增/变更的 key 列表”。
* 任何 PR 如导致缺失翻译或键名冲突，视为不通过。
* 引入第三方翻译时，需人工校对关键页面（首页、导航、设置）。

---

## 5. 任务边界与安全

* **不输出敏感凭据**：密钥、Token、私钥、内部地址等必须打码或示例化（如 `example.com`）。
* 对破坏性或收费操作，必须给出**替代方案**或**明确提示成本**。
* 资料来源不确定时，明确写出“不确定/需验证”，并给出验证路径（例如本地运行命令、官方文档入口）。

---

## 6. 与 Tralume 主题相关的默认假设

* 主题使用 **Material Design 3** 风格组件（`material-esm/material`）。
* 以 **零构建/低耦合** 为优先：优先通过 import maps 与 Web Components 集成。
* 提供 **示例站点（exampleSite）** 与 **最小配置**，确保“克隆即可运行”。

### 6.1 亚克力 / 玻璃拟态规范（必须）

* 所有主要块级表面（导航、卡片、页脚、浮动按钮、代码块等）须优先使用 `--app-glass-*` 变量组合：  
  * 背景：`var(--app-glass-surface-color | --app-glass-elevated-color | --app-glass-strong-color)`；  
  * 描边：`var(--app-glass-border-color | --app-glass-border-strong-color)`；  
  * 模糊：统一调用 `backdrop-filter: blur(var(--app-glass-blur-radius))`（含 `-webkit-`）。  
  新增组件若具卡片/浮层语义，必须使用上述变量，禁止直接引用 `--md-sys-color-surface-*`。
* 透明度仅通过 `--app-glass-surface-alpha` 等变量调节，不得在组件内部硬编码百分比。若需特殊层级，先评估是否可以复用 `surface / elevated / strong` 三档。
* **主题色功能已统一为亚克力方案**：禁止新增/恢复 `data-theme-color`、色板预设、背景动态取色等逻辑。色彩变化仅能依赖默认 MD3 Token 与 `--app-glass-*` 变量。
* 设置面板的“磨砂强度”滑块（`data-glass-strength-range`）负责写入 CSS 变量；  
  * 默认值 45%，范围 15%–95%，变更会同时更新多层 alpha 与边框。  
  * **不提供任何“关闭亚克力”入口**，禁止移除模糊或回落到纯色表面。
* 需要在 JS 中读取用户偏好时：  
  * 透明度存储键：`tralume-glass-strength`；  
  * 默认档位映射于 `data-glass-strength="soft|balanced|bold"`，如需新增预设需同步更新 `layouts/_default/baseof.html` 与设置面板文案。
* 对不支持 `color-mix` / `backdrop-filter` 的浏览器，组件应保持可读性：  
  * 继续提供阴影与描边；  
  * 若添加新特性，务必在 PR 说明中列出降级策略或验证路径（如启用 Firefox flag）。
* 添加/修改表面时，请自检是否需要暴露新变量或设置项；若答案为是，需同步更新 `assets/js/main.js`、`i18n/*` 与本文件。

---

## 7. 交付物清单（面向 Agent）

当被要求“实现/修改某功能”时，交付内容应包含：

1. **变更摘要**：一句话说明本次修改达成的目标。
2. **目录/文件影响**：列表指出新增/修改/删除的路径。
3. **使用说明**：在何处启用或配置（以中文说明）。
4. **验证步骤**：本地如何预览或测试成功（含预期结果）。
5. **回滚提示**：如何快速恢复到变更前状态（命令或操作步骤）。
6. **Git 提交信息**：提供建议的 `git commit message`（优先遵循仓库现有风格，如 `feat(scope): ...` / `fix(scope): ...` / `style: ...`）。

---

## 8. 常见错误清单（快速自查）

* 回答非简体中文 → 不合规。
* 代码无中文注释/无风险提示 → 不合规。
* 模板写死中文文案 → 不合规（必须走 i18n）。
* 给出大段无关背景或营销语言 → 不合规（保持简洁）。

---

## 9. 版本与维护

* 本文件与主题版本保持同步。重大变更需在 **CHANGELOG** 记录，并在 **README** 标注“Agent 规范已更新”。
* 若项目策略（语言、i18n 结构、依赖）发生变化，须第一时间更新本文件。

---

**最后更新**：2025-12-14（UTC+8）
