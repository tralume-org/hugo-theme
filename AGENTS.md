# AGENTS.md

> 协作型 AI 行为与输出规范，用于 **Tralume（Hugo 主题）** 项目。

`docs/` 有独立的规范文件 `docs/AGENTS.md`，改动时请同时遵循。

---

## 1. 语言

* **默认语言：简体中文**。仅当任务明确要求时使用其他语言。
* **文档语言一致**：
  * `README.md`：仅英文。
  * `README.zh-Hans.md`：仅简体中文（产品名/代码标识除外）。
  * `i18n/zh-Hans.yaml`：仅简体中文，无英文占位文案。
* **模板写死文案一律禁止**，必须走 i18n key。
* **代码注释**：中文，说明意图与易错点，外文标识原样保留。

---

## 2. 输出结构

* 结论先行 → 步骤/方案 → 常见坑（可选）→ 后续扩展（可选）。
* 列表项不超过两级。
* 破坏性操作前必须给出风险提示与回滚方式。
* 样例最小化：只给能运行的最小片段。

---

## 3. i18n 规范

* 资源：`i18n/zh-Hans.yaml`、`i18n/en-US.yaml`；语言配置在 `hugo.toml` 的 `[languages]` 段。
* 避免拼接句，尽量一整句为一个 key；占位符用具名形式。
* 日期/数字/货币走 Hugo 本地化方法，禁止写死格式。
* 新增/修改文案：新增 key → 更新中英文翻译 → PR 描述列出变更 key。
* 翻译缺失或键名冲突视为不通过。

---

## 4. 任务边界

* 不输出敏感凭据；破坏性/收费操作须给出替代方案。
* **默认不做向后兼容**：以最佳实践 + 最小复杂度为准，可删除过时代码。
* **单文件单职责**：按功能/组件拆分，禁止堆叠不相关逻辑。
* **测试输出必须写入 `public_test/`**，禁止写入 `public/`。

---

## 5. 主题约定

* **Material Design 3** 色彩系统 + Material Symbols Rounded 图标字体。
* **零构建/低耦合**：CSS/JS 通过 Hugo 内置资源管道处理，无外部构建依赖。
* **亚克力表面**：统一使用 `app-glass` / `app-card` 通用类（`glass-surfaces.css`），禁止各组件手写亚克力声明。透明度/圆角/模糊均走 CSS 变量，禁止硬编码。
* **外观设置面板**：磨砂强度、磨砂模糊度、背景模糊度均提供滑块调节；不提供"关闭亚克力"入口。
* **主题色**：前端动态 token 系统（种子色 → MD3 色板），含色板预设与自定义颜色选择器。
* **降级**：不支持 `backdrop-filter` 时依赖阴影与描边保持可读性。

---

## 6. 交付物清单

1. **变更摘要**：一句话目标。
2. **文件影响**：新增/修改/删除的路径。
3. **使用说明**：如何启用或配置。
4. **验证步骤**：本地测试方法与预期结果。
5. **回滚提示**：快速恢复的命令或步骤。
6. **Git 提交信息**：遵循 `feat(scope): ...` / `fix(scope): ...` / `style: ...`。

---

## 7. 快速自查

* 回答非简体中文 → 不合规。
* 代码无中文注释 → 不合规。
* 模板写死文案 → 不合规。
* 大段无关背景 → 不合规。

---

**最后更新**：2026-06-06（UTC+8）
